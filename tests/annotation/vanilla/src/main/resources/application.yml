spring:
  application:
    name: vanilla
server:
  port: 8080

management:
  context-path: /admin
  
security:
  user:
    password: password
  oauth2:
    resource:
      filter-order: 3
logging:
  level:
    org.springframework.security: WARN