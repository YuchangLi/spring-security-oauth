spring:
  application:
    name: client
server:
  port: 8081
  # use a different path for the client (otherwise cookies are shared)
  context-path: /client
security:
  user:
    password: password
#  basic:
#    enabled: false
  oauth2:
    resource:
      filter-order: 3
